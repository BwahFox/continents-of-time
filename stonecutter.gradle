plugins {
	id 'dev.kikugie.stonecutter'
}

// The version src/ is currently switched to. `./gradlew "Set active project to 1.20.1"` switches (it rewrites
// the comment conditions in place); reset to 26.2 before committing.
stonecutter.active '26.2'
