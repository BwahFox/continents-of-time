accessWidener v2 official

# The atlas extends the vanilla noise generator so the server builds the real overworld RandomState for its dimension
# (the modern era needs it). Moderner Beta widens the same class for the same reason.
extendable class net/minecraft/world/level/levelgen/NoiseBasedChunkGenerator

# The client's fallback climate reads a vanilla biome's temperature and downfall the way its colormaps do.
accessible class net/minecraft/world/level/biome/Biome$ClimateSettings
accessible field net/minecraft/world/level/biome/Biome climateSettings Lnet/minecraft/world/level/biome/Biome$ClimateSettings;
