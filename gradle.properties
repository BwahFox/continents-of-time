org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configuration-cache=false
# Stonecutter with Groovy build scripts (acknowledged).
dev.kikugie.stonecutter.hard_mode=true

# One JDK for every target: Java 25 compiles the 26.2 build natively and the 1.20.1 build with --release 17.
org.gradle.java.home=/usr/lib/jvm/java-25-openjdk

loom_version=1.17-SNAPSHOT
loader_version=0.19.5

mod_version=1.0.2
group=dev.continentsoftime

# Per-Minecraft-version values (minecraft version, Fabric API, Moderner Beta, Java) live in versions/<mc>/gradle.properties.
